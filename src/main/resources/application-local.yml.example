# Local overrides (copy to application-local.yml). Never commit real secrets.
hft:
  mode: LIVE
  polymarket:
    auth:
      signature-type: 1
      funder-address: "0x..."
      private-key: "0x..."
      api-key: "..."
      api-secret: "..."
      api-passphrase: "..."
